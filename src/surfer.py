# Use either BS4 or Selenium
def wiki_surf(source: str, destination: str):
    """
    Given two URLs to Wikipedia articles `source` and `destination`, where:

        `source` is a URL to a Wikipedia article, which contains hyperlinks,
        `destination` is a URL to a Wikipedia article

    Get from `source` to `destination` in as few attempts as possible.
    """
    pass
